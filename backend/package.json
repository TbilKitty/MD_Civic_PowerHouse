{
  "name": "maryland-legislative-watch-subscriptions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply SUBSCRIBERS --local",
    "db:remote": "wrangler d1 migrations apply SUBSCRIBERS --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
